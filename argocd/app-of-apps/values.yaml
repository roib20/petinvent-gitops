# Default values for application.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates

spec:
  destination:
    server: https://kubernetes.default.svc
  source:
    repoURL: https://github.com/roib20/argocd-test
    targetRevision: main
