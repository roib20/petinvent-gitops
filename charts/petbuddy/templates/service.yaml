apiVersion: v1
kind: Service
metadata:
  name: {{ include "petbuddy.fullname" . }}
  labels:
    {{- include "petbuddy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "petbuddy.selectorLabels" . | nindent 4 }}
