app:
  secret:
    existingSecret: petbuddy-secret

# Values for PostgreSQL child/dependency chart
postgresql-ha:
  postgresql:
    existingSecret: petbuddy-secret
    username: user
    database: db
    repmgrUsername: repmgr
